export interface Frames {
  id: string;
  order: number;
  title: string;
}

export interface Todo {
  id: string;
  description: string;
}

export interface TodoData {
  id: string;
  description: string;
  done: boolean;
}
